<%= render partial: "application/edit_object_instructions", locals: { object: @resource } %>

<div class="form">

  <%= form_for @subject do |f| %>

    <%= render partial: "application/form", locals: {f: f, category_id: @subject.category.id } %>
  <% end %>
  
</div>