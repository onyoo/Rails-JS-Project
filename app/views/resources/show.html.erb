Category: <%= link_to @resource.category.name, category_path(@resource.category) %> <br>

Subject: <%= link_to @resource.subject.name, subject_path(@resource.subject) %> <br>

<%= link_to "Edit this resource.", edit_resource_path %> <br>


<%= render partial: "index", locals: { resource: @resource } %>
