<% if !defined? object.category %>

  <%= object.name %>  <!-- Category name -->

<% else %>

  <%= link_to object.category.name, category_path(object.category) %>

   > <!-- do not edit! Relation marker!! -->

  <% if !defined? object.subject %>
  
    <%= object.name %>  <!-- Subject name -->

  <% else %>

    <%= link_to object.subject.name, subject_path(object.subject) %>

     > <!-- do not edit! Relation marker!! -->

    <%= object.name %>  <!-- Resource name -->

  <% end %>

<% end %>
