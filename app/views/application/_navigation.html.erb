<% if defined? resource %>

  <%= link_to resource.category.name, category_path(resource.category) %> > <%= link_to resource.subject.name, subject_path(resource.subject) %> > <%= resource.name %> <br><br>

<% end %>

<% if defined? subject %>

<%= link_to subject.category.name, category_path(subject.category) %> > <%= subject.name %> <br><br>

<% end %>

<% if defined? category %>

  <%= category.name %>

<% end %>